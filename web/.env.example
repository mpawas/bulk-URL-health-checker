# Browser → API (EventSource + POST /batches). Must be reachable from the user's machine.
NEXT_PUBLIC_API_URL=http://localhost:4000

# Server Components: use this on the host. Docker Compose overrides it to
# http://api:4000 so the web container talks to the api service by name.
API_URL_INTERNAL=http://localhost:4000
