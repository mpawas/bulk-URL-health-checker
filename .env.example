# Copy to .env before `docker compose up`.
# Compose still overrides DATABASE_URL / REDIS_URL / PORT / Next URLs per service
# (see docker-compose.yml). Per-package templates:
#   api/.env.example
#   worker/.env.example
#   web/.env.example

NODE_ENV=development
